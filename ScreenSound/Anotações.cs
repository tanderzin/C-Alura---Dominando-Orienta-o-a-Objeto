Variável =  Atributos (string, int, float (números decimais/quebrados necessidade 'f'), double, bool)

Função = Método

Construtores = ?

Instância = ?

Condicionais = (if, else)

Loops = for (int i = 0 (Declaração da variável); i < length (Condição); i++ (Repetição) ){ }
		foreach (var item (Declaração da variável) in collection (Repetição) ){ }
		Em Array = .Length
		Em List/Dicionary = .Count

Acessadores = sem a declarável (public) ele é (private),
			  acessar um método em outra classe criar uma variável
			  (Classe(Nome da classe que queira acessar) nomeQualquer;)
			  (nomeQualquer.MétodoDaClasse();)
			  